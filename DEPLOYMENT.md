# Инструкция по развертыванию Web App в production

## 1. Подготовка базы данных

### Выполнение миграций

Выполните SQL миграцию для создания необходимых таблиц и колонок:

```bash
mysql -h mysql -u shebo -p shebo < migrations/add-webapp-tables.sql
```

Или через Docker:

```bash
docker exec -i <mysql_container_name> mysql -u shebo -p shebo < migrations/add-webapp-tables.sql
```

Миграция создаст:
- Таблицу `phrases` для хранения стоп-фраз
- Колонку `isAdmin` в таблице `users_shebo` для определения администраторов

## 2. Настройка переменных окружения

Убедитесь, что в `.production.env` установлены следующие переменные:

```env
NODE_ENV=production

DB_HOST=mysql
DB_PORT=3306
DB_USER=shebo
DB_PASS=zussVzo-0S
DB_NAME=shebo

BX24_OUTGOING_TOKEN=...
BX24_INCOMING_TOKEN=...
BX24_INCOMING_USER=...
BX24_DOMAIN=https://shebo.bitrix24.ru

TELEGRAM_BOT_TOKEN=...

APP_URL=https://shebo.h512.ru
```

**Важно:** `APP_URL` должен указывать на ваш production домен (https://shebo.h512.ru).

## 3. Сборка и запуск приложения

### Сборка проекта

```bash
npm run build
```

### Запуск в production режиме

```bash
npm run start:prod
```

Или через PM2 (рекомендуется):

```bash
pm2 start dist/main.js --name bitrix-app
pm2 save
pm2 startup
```

## 4. Проверка работы

### Проверка маршрутов

- Главная страница: `https://shebo.h512.ru/`
- Web App: `https://shebo.h512.ru/webapp`
- API статуса: `https://shebo.h512.ru/webapp/api/status`

### Проверка Telegram бота

1. Откройте бота в Telegram
2. Проверьте, что в описании бота есть информация о Mini App
3. Проверьте, что в меню бота (гамбургер) есть кнопка "📊 Мониторинг"
4. Проверьте, что команды (`/start`, `/info`, `/help`) отображаются в меню

### Проверка Mini App

1. Откройте бота в Telegram
2. Нажмите на кнопку "📊 Мониторинг" в меню
3. Должна открыться страница с информацией о статусе системы
4. Если вы администратор, должна быть доступна вкладка "Фразы"

## 5. Настройка администраторов

Для назначения пользователя администратором выполните SQL запрос:

```sql
UPDATE users_shebo SET isAdmin = TRUE WHERE bitrix_id = <ID_ПОЛЬЗОВАТЕЛЯ>;
```

Или через несколько пользователей:

```sql
UPDATE users_shebo SET isAdmin = TRUE WHERE bitrix_id IN (114, 120);
```

## 6. Загрузка начальных фраз

При первом запуске приложения фразы из файла `comment-stop-phrases.txt` автоматически загружаются в базу данных через `PhrasesService.initializeFromFile()`.

Если нужно загрузить фразы вручную, убедитесь, что файл `comment-stop-phrases.txt` существует в корне проекта.

## 7. Мониторинг и логи

Проверьте логи приложения для отслеживания работы:

```bash
# Если используете PM2
pm2 logs bitrix-app

# Или через systemd
journalctl -u bitrix-app -f
```

## 8. Обновление приложения

При обновлении кода:

1. Остановите приложение:
   ```bash
   pm2 stop bitrix-app
   ```

2. Обновите код из репозитория:
   ```bash
   git pull origin main
   ```

3. Установите зависимости (если изменились):
   ```bash
   npm install
   ```

4. Выполните сборку:
   ```bash
   npm run build
   ```

5. Запустите приложение:
   ```bash
   pm2 restart bitrix-app
   ```

## 9. Troubleshooting

### Проблема: Mini App не открывается

- Проверьте, что `APP_URL` в `.production.env` указывает на правильный домен
- Проверьте, что домен доступен извне (не localhost)
- Проверьте SSL сертификат (должен быть валидный HTTPS)

### Проблема: Валидация initData не проходит

- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте логи сервера для детальной информации
- Убедитесь, что пользователь существует в БД и связан с telegram_id

### Проблема: Кнопка меню не отображается

- Проверьте логи при запуске - должна быть запись "Кнопка меню Mini App установлена успешно"
- Если не поддерживается, кнопка не будет установлена, но описание бота будет обновлено

## 10. Структура файлов

```
bitrix/
├── migrations/
│   └── add-webapp-tables.sql      # Миграция для БД
├── views/
│   └── webapp.hbs                 # Шаблон Web App
├── src/
│   ├── webapp/                    # Модуль Web App
│   ├── phrases/                   # Модуль управления фразами
│   └── telegram/                  # Обновленный модуль Telegram
└── .production.env                # Production конфигурация
```

